Profiling data collection and analysis, pc, roofline and importance analysis. prompt has 5 major regions, code, stats, machines specific guardrails, general pitfall avoidance guradrails and output format definition. The framework replaces the optimized code automatically to build and runs every version 10 times to get the runtime, The correctness check is done by 2 ways: if the baseline have numbers, the optimized ones produce same numbers and if the benchmark have in-built check, it uses that output (tailored for every application) to check correctness. 
memory-bound (shmembench) and compute-bound (bitonic-sort, md5hash, atan2) workloads. 