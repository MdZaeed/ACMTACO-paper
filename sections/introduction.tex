GPU optimization is hard: requires understanding memory hierarchy, warp scheduling, instruction-level behavior. expert tuners spend weeks on what an LLM might attempt in seconds. existing code LLM benchmarks (HumanEval, MBPP, SWE-bench) test algorithmic correctness, not low-level performance. profiling data is what a expert would look at first.contribution: end-to-end automated loop-profile-prompt-optimize-build → measure → iterate